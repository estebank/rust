// run-rustfix

trait T {
    unsafe fn foo(a: &usize, b: &usize) -> usize;
    fn bar(&self, a: &usize, b: &usize) -> usize;
}

mod foo {
    use super::T;
    impl T for () {    fn bar(&self, _: &usize Ref(ReLateBound(DebruijnIndex(0), BrAnon(1)), usize, Not), _: &usize Ref(ReLateBound(DebruijnIndex(0), BrAnon(2)), usize, Not)) -> usize { todo!() }
        unsafe fn foo(_: &usize, _: &usize Ref(ReLateBound(DebruijnIndex(0), BrAnon(1)), usize, Not)) -> usize { todo!() }
    } //~ ERROR not all trait items

    impl T for usize { //~ ERROR not all trait items
        fn bar(&self, _: &usize Ref(ReLateBound(DebruijnIndex(0), BrAnon(1)), usize, Not), _: &usize Ref(ReLateBound(DebruijnIndex(0), BrAnon(2)), usize, Not)) -> usize { todo!() }
        unsafe fn foo(_: &usize, _: &usize Ref(ReLateBound(DebruijnIndex(0), BrAnon(1)), usize, Not)) -> usize { todo!() }
    }
}

fn main() {}
